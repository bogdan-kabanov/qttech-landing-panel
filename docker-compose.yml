version: '3.8'

services:
  app:
    image: ${DOCKER_USERNAME:-your-username}/qttech-landing-panel:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qttech-landing-panel
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8003}:80"
    env_file:
      - .env
    volumes:
      - app-storage:/var/www/html/storage
      - ./.env:/var/www/html/.env
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - app-network

  mysql:
    image: mysql:8.0
    container_name: qttech-landing-mysql
    restart: unless-stopped
    ports:
      - "${DB_PORT:-3308}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-secret}
      MYSQL_DATABASE: ${DB_DATABASE:-goride_landing}
      MYSQL_USER: ${DB_USERNAME:-landing}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

volumes:
  mysql-data:
  app-storage:

networks:
  app-network:
    driver: bridge
